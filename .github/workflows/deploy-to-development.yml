name: Deploy to Development

on:
  pull_request:
    branches:
      - development
    types:
      - closed

jobs:
  deploy-development:
    runs-on: ubuntu-latest
    environment: development
    if: github.event.pull_request.merged == true

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 7.0.x

      # Bygg & publicera (ändra path/projekt vid behov)
      - name: Publish
        run: dotnet publish ./YourSolution.sln -c Release -o ./publish.github/workflows/deploy-to-development.yml

      # TODO: ersätt detta med din riktiga deploy
      - name: Deploy to Development Environment
        run: |
          echo "Deploying to Development Environment..."
          # ex: scp/ssh/az webapp deploy/docker push osv.
